// export digunakan untuk mengexpose module ke luar module agar bisa digunakan
export function sayHello(name) {
   console.info(`Hello ${name}`); 
}

export function sayGoodbye(name) {
   console.info(`Goodbye ${name}`); 
}